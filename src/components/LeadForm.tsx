'use client';
import { CheckCircle } from 'lucide-react';
import { FormEvent, useState } from 'react';
import { trackEvent } from '@/lib/analytics';
export function LeadForm(){const[ok,setOk]=useState(false);function submit(e:FormEvent){e.preventDefault();setOk(true);trackEvent('footer_lead_completed')}return <section id="contacto" className="bg-slate-50 px-4 py-12"><div className="mx-auto grid max-w-7xl gap-6 rounded-3xl border bg-white p-6 md:grid-cols-[1fr_2fr_1fr]"><div><h2 className="text-2xl font-black">¿Prefieres que nosotros te contactemos?</h2><p className="mt-2 text-slate-600">Déjanos tus datos y un asesor se pondrá en contacto contigo.</p></div><form onSubmit={submit} onFocus={()=>trackEvent('footer_lead_started')} className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><input required aria-label="Nombre" placeholder="Nombre completo" className="focus-ring min-h-12 rounded-lg border px-3"/><input required aria-label="Teléfono o WhatsApp" placeholder="Teléfono / WhatsApp" className="focus-ring min-h-12 rounded-lg border px-3"/><input required type="email" aria-label="Correo electrónico" placeholder="Correo electrónico" className="focus-ring min-h-12 rounded-lg border px-3"/><select required aria-label="Qué deseas asegurar" className="focus-ring min-h-12 rounded-lg border px-3"><option>Auto</option><option>Moto</option><option>Pickup</option><option>Otro</option></select><label className="text-sm sm:col-span-2"><input required type="checkbox"/> He leído y acepto el Aviso de Privacidad.</label><button className="focus-ring min-h-12 rounded-xl bg-[#b0007a] px-5 font-bold text-white lg:col-span-2">Quiero que me contacten</button></form><div aria-live="polite" className="rounded-2xl bg-teal-50 p-5">{ok?<><CheckCircle className="text-[#008f8b]"/><strong>Gracias. Recibimos tus datos.</strong><p className="text-sm text-slate-600">Pronto un asesor te contactará.</p></>:<p className="text-slate-600">Formulario DEMO preparado para futura integración con Supabase.</p>}</div></div></section>}
